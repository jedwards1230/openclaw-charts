#!/bin/bash
# Hook: Stop
# Fires when Claude finishes a response.
# NOTE: The actual Helm lint quality checks are in chart-version-check.sh
# and helm-lint-check.sh (registered separately in settings.json).

set -euo pipefail

exit 0
